server:
  port: 8100

spring:
  application:
    name: eureka
  security:
    user:
      name: root
      password: bacon123

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/